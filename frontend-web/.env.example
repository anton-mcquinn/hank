# API base URL — defaults to the production droplet (https://hank.idleworkshop.com/api/v1) if unset.
# Override here only when pointing at a local backend, e.g.:
# VITE_API_URL=http://localhost:8000/api/v1
